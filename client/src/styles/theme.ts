import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e1e1e',
    },
    secondary: {
      main: '#1976d2',
    },
    background: {
      default: '#f4f4f4',
    },
  },
});

export default theme;
